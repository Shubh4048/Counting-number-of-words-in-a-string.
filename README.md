# Counting Number of Words in a String (Python)

This repository contains a simple Python program to count the number of words present in a given string.

Counting words is a common beginner problem that helps understand string manipulation, input handling, and basic Python logic.

---

## 🔍 Problem Statement

Given a string, write a Python program to count the total number of words in it.

A **word** is defined as a sequence of characters separated by spaces.

---

## 💡 Approach

1. Take input string from the user.
2. Remove leading and trailing spaces (if any).
3. Split the string using whitespace as a delimiter.
4. Count the number of elements obtained after splitting.

---

## 🧠 Algorithm

1. Read the input string.
2. Use the `strip()` method to remove extra spaces.
3. Use the `split()` method to separate words.
4. Calculate the length of the resulting list.
5. Print the word count.
